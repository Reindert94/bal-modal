import { h } from '@stencil/core';
import { BEM } from '../../../utils/bem';
export const OverviewLink = ({ item, isMenu, onClick }) => {
  const block = BEM.block('nav');
  if (!item) {
    return '';
  }
  return (h("a", { class: Object.assign(Object.assign({}, block.element('mobile-overview-link').class()), block.element('mobile-overview-link').modifier('menu').class(!!isMenu)), href: item.href, target: item.target, onClick: ev => onClick(ev) }, item.label));
};
