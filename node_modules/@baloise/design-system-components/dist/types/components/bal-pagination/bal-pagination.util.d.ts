export type PaginationControlType = 'page' | 'dots';
export type PaginationControl = {
  type: PaginationControlType;
  label: string;
  active: boolean;
};
export declare function generatePaginationControl(value: number, totalPages: number, pageRange: number): any;
