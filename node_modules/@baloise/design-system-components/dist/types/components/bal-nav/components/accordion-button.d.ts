import { FunctionalComponent } from '../../../stencil-public-runtime';
export interface AccordionButtonProps {
  id: string;
  label: string;
  level: 'meta' | 'menu';
  open: boolean;
  link: boolean;
  href?: string;
  target?: string;
  onClick: (ev: MouseEvent) => void;
}
export declare const AccordionButton: FunctionalComponent<AccordionButtonProps>;
