import { ListenerAbstract } from '../types/listener';
import { BalFocusInfo } from './focus.interfaces';
export declare class BalFocusListener<TObserver> extends ListenerAbstract<TObserver, BalFocusInfo> {
  connect(el: HTMLElement): void;
  disconnect(): void;
  private onFocusin;
  private onFocusout;
}
