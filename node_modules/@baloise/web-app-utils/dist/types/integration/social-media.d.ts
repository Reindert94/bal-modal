import { Language } from '../language/index';
import { Region } from './app-integration';
export interface SocialMediaLink {
    label: string;
    link: string;
}
export declare const loadSocialMediaLinks: (lang: Language, region?: Region) => Promise<SocialMediaLink[]>;
