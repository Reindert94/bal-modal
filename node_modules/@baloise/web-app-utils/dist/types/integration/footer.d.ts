import { Language } from '../language/index';
import { Region } from './app-integration';
export interface FooterLink {
    label: string;
    link: string;
}
export declare const loadFooterLinks: (lang: Language, region?: Region) => Promise<FooterLink[]>;
